# Browser evidence

`review-workspace.png` is an actual desktop viewport capture of the local FinAgent application after a completed fictional review was loaded from persistence. It is not a mockup. Credential inputs were empty when captured. The complete browser flow and result are documented in `../../VALIDATION.md`.
